{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissoes da janela principal. O app so precisa do nucleo: toda a I/O acontece no Rust, nunca no webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
